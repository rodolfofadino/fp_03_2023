@using fiap.ViewComponents;
@model IEnumerable<Noticia>

<ul>
    @foreach (var item in Model)
    {
        <li >
            <h3> @item.Titulo</h3>
            <img src="@item.Imagem">
            
        </li>
    }

    
</ul>